# Project Template

Шаблон проекта на Next.js 16 с архитектурой Evolution Design. Готов к использованию с минимальной настройкой: TanStack Query, Axios, TypeScript и Tailwind CSS уже настроены.

## 🚀 Быстрый старт

```bash
npm install          # или bun install
npm run dev          # или bun dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 📁 Архитектура

Проект организован согласно **Evolution Design (ED small)** - архитектурному паттерну для веб-приложений.

### Структура слоёв

```
src/
├── app/          # Весь роутинг, главные файлы страниц
├── features/     # Основной слой - крупные куски функциональности
├── services/     # Бизнес-сущности и сервисы для нескольких фич
└── shared/       # Переиспользуемый код
    ├── lib/      # Утилиты, клиенты, конфигурация
    ├── ui/       # Переиспользуемые UI компоненты
    ├── types/    # Общие типы
    └── model/    # Общая бизнес-логика (если нужна)
```

### Где что хранить

#### `app/` - Точка входа

- **Роутинг**: файлы страниц (`page.tsx`), layouts (`layout.tsx`)
- **Глобальная конфигурация**: `layout.tsx`, `globals.css`
- **Композиция фич**: страницы, которые объединяют несколько фич

#### `features/` - Основной слой

Каждая папка = **крупный независимый кусок функциональности**.

**Важно**: Не делайте фичи мелкими! Нормально иметь 3-4 фичи на старте.

Примеры фич:

- `auth/` - аутентификация и авторизация
- `todo-list/` - управление задачами
- `profile/` - профиль пользователя
- `dashboard/` - дашборд

**Структура фичи**:

Начните с простой структуры - все файлы на одном уровне:

```
feature-name/
├── index.ts          # Public API
├── api.ts            # API функции
├── use-*.ts          # Хуки для запросов
├── types.ts          # Типы фичи
└── *.tsx             # Компоненты
```

Для больших фич можно группировать по типу файлов:

```
feature-name/
├── index.ts          # Public API
├── components/       # UI компоненты
├── hooks/            # React хуки
├── types/            # TypeScript типы
├── utils/            # Утилиты
└── config/           # Конфигурация
```

#### `services/` - Бизнес-сущности

**Когда использовать**: Код, который используется в **нескольких фичах** и представляет бизнес-сущность или сервис.

**Критерии для выноса в `services`**:

- Используется в 2+ фичах
- Представляет бизнес-сущность (пользователь, уведомления, платежи)
- Имеет свою бизнес-логику и API

**Примеры**:

- `user-service/` - работа с пользователями (используется в `profile/`, `dashboard/`, `settings/`)

  ```typescript
  // Используется в features/profile, features/dashboard, features/settings
  userService.getCurrent()
  userService.update()
  ```

- `notification-service/` - уведомления (используется в `dashboard/`, `habits/`, `todo-list/`)

  ```typescript
  // Используется в нескольких фичах
  notificationService.send()
  notificationService.markAsRead()
  ```

- `analytics-service/` - аналитика (используется везде для трекинга)

**Важно**: Если код используется только в одной фиче - оставьте его в `features/`. Не создавайте `services` заранее, выносите только когда появляется реальная необходимость.

#### `shared/` - Переиспользуемый код

- **`lib/`** - утилиты, клиенты, конфигурация
  - `api-client.ts` - настроенный axios
  - `query-client.tsx` - провайдер TanStack Query
  - `query-keys.ts` - фабрика ключей для запросов
  - `cn.ts` - утилита для классов

- **`ui/`** - переиспользуемые UI компоненты (Button, Input, Modal и т.д.)

- **`types/`** - общие TypeScript типы

- **`model/`** - общая бизнес-логика (если нужна)

## 🛠 Технологии

- **Next.js 16** - React фреймворк
- **React 19** - UI библиотека
- **TypeScript** - строгая типизация
- **TanStack Query** - управление серверным состоянием
- **Axios** - HTTP клиент
- **Tailwind CSS** - стилизация
